# RC4.6.3 Full Date Picker Click

- Period Start, Period End, and Pay Date now open the native date picker when clicking anywhere inside the field.
- Applies to Kitchen Group Payroll and Add Manual Payroll dialogs.
- Includes focus fallback for browsers without `showPicker()` support.
