RESTAPAY STYLE D - COMPACT ENTERPRISE

Updated:
- Global cards, buttons, fields, tables, icons, dialogs, and page spacing
- Dashboard command tiles and KPI cards converted to slim pastel enterprise style
- Sidebar structure retained
- Scrollbars made thin and transparent
- Existing payroll, Toast, Supabase, invoice, vendor, and calculation logic left unchanged

Local verification:
1. npm install --registry=https://registry.npmjs.org
2. npm run build
3. npm run dev

The automated build could not be completed in the packaging environment because its internal npm mirror did not provide one locked dependency. No JavaScript source logic was modified; the design update is contained in src/styles.css.
