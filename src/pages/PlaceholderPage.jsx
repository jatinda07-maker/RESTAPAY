export default function PlaceholderPage({ title }) {
  return (
    <div className="placeholder-page">
      <h1>{title}</h1>
      <p>This page is part of the new UI project and will be built next using the same component system.</p>
    </div>
  )
}
